# вспомогательный файл-фикстура без расширения .atm,
# используется тестом PAR_6008_use_load_fails.atm: он существует на диске,
# поэтому resolve_canonical_path успешно резолвит путь (PAR_6007 не сработает),
# но load_file бросает PreparationError(PRE_0002), так как расширение не .atm -
# это перехватывается в parse_use() и оборачивается в PAR_6008.
func main(){
}
