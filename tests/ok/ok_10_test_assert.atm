func add(a -> Int, b -> Int) -> Int { return a + b; }

func main(){
    return;
}

test AddWork {
    assert add(2, 2) == 4;
    assert add(-1, 1) == 0;
}

test StringConcat {
    assert "hello" + "world" == "helloworld";
}
